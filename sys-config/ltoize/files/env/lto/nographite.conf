#TODO: refactor this
NOGRAPHITE="-fno-graphite-identity -fno-tree-loop-distribution -fno-loop-nest-optimize"
CFLAGS="${CFLAGS} ${NOGRAPHITE}" 
CXXFLAGS="${CXXFLAGS} ${NOGRAPHITE}"
LDFLAGS="${LDFLAGS} ${NOGRAPHITE}"
